import { StyleSheet } from 'react-native';

export const createLocalStyles = (colors: any, isDarkMode: boolean = false) => StyleSheet.create({
  backdrop: {
    flex: 1,
    backgroundColor: 'rgba(0,0,0,0.6)',
    justifyContent: 'flex-end',
  },
  centeredView: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'rgba(0,0,0,0.5)',
  },
  modal: {
    backgroundColor: colors.surface || (isDarkMode ? '#2a2a2a' : '#f5f5f5'),
    padding: 20,
    borderTopLeftRadius: 20,
    borderTopRightRadius: 20,
    flexDirection: 'row',
    justifyContent: 'space-around',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: -2 },
    shadowOpacity: isDarkMode ? 0.3 : 0.1,
    shadowRadius: 4,
    elevation: 5,
  },
  modalView: {
    backgroundColor: colors.surface || (isDarkMode ? '#2a2a2a' : '#f5f5f5'),
    borderRadius: 20,
    padding: 24,
    width: '85%',
    maxWidth: 400,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: isDarkMode ? 0.4 : 0.25,
    shadowRadius: 8,
    elevation: 8,
  },
  modalTitle: {
    fontSize: 22,
    fontWeight: 'bold',
    marginBottom: 20,
    textAlign: 'center',
    color: colors.text || (isDarkMode ? '#ffffff' : '#1a1a1a'),
  },
  iconButton: {
    alignItems: 'center',
    padding: 16,
    backgroundColor: colors.background || (isDarkMode ? '#1a1a1a' : '#ffffff'),
    borderRadius: 16,
    minWidth: 100,
    borderWidth: 1,
    borderColor: colors.border || (isDarkMode ? '#404040' : '#e0e0e0'),
  },
  iconLabel: {
    marginTop: 8,
    fontSize: 14,
    color: colors.text || (isDarkMode ? '#ffffff' : '#1a1a1a'),
    textAlign: 'center',
    fontWeight: '600',
  },
  button: {
    backgroundColor: colors.background || (isDarkMode ? '#1a1a1a' : '#ffffff'),
    padding: 16,
    borderRadius: 12,
    marginVertical: 8,
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'center',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: isDarkMode ? 0.3 : 0.1,
    shadowRadius: 4,
    elevation: 3,
    borderWidth: 1,
    borderColor: colors.border || (isDarkMode ? '#404040' : '#e0e0e0'),
  },
  buttonText: {
    fontSize: 16,
    fontWeight: '600',
    color: colors.text || (isDarkMode ? '#ffffff' : '#1a1a1a'),
  },
  inputLabel: {
    fontSize: 14,
    fontWeight: '600',
    marginTop: 12,
    marginBottom: 8,
    color: colors.text || (isDarkMode ? '#ffffff' : '#1a1a1a'),
  },
  input: {
    borderWidth: 1,
    borderColor: colors.border || (isDarkMode ? '#404040' : '#e0e0e0'),
    borderRadius: 12,
    padding: 14,
    fontSize: 16,
    marginBottom: 12,
    backgroundColor: colors.background || (isDarkMode ? '#1a1a1a' : '#ffffff'),
    color: colors.text || (isDarkMode ? '#ffffff' : '#1a1a1a'),
  },
  errorText: {
    color: '#ff4444',
    marginBottom: 12,
    textAlign: 'center',
    fontSize: 14,
    fontWeight: '600',
  },
  buttonRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginTop: 16,
    gap: 12,
  },
  cancelButton: {
    backgroundColor: colors.background || (isDarkMode ? '#1a1a1a' : '#ffffff'),
    flex: 1,
  },
  submitButton: {
    backgroundColor: colors.primary || (isDarkMode ? '#3b82f6' : '#2563eb'),
    flex: 1,
  },
  debugButton: {
    backgroundColor: colors.background || (isDarkMode ? '#1a1a1a' : '#ffffff'),
    padding: 16,
    borderRadius: 12,
    marginVertical: 8,
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'center',
    borderWidth: 1,
    borderColor: colors.border || (isDarkMode ? '#404040' : '#e0e0e0'),
  },
  debugButtonText: {
    fontSize: 14,
    fontWeight: '600',
    color: colors.text || (isDarkMode ? '#ffffff' : '#1a1a1a'),
  },
});

// Keep backward compatibility
const localStyles = createLocalStyles({}, false);
export default localStyles;